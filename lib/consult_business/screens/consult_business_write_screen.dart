import 'package:flutter/material.dart';

class ConsultBusinessWriteScreen extends StatelessWidget {
  const ConsultBusinessWriteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
